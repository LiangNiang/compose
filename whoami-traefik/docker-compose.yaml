version: '3'
services:
  whoami:
    image: containous/whoami
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.whoami.rule=Host(`whoami.devdoll.art`)"
networks:
  default:
    name: traefik_default
    external: true